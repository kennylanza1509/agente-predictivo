# Dependencias del proyecto.

# Servidor MCP (Model Context Protocol): expone las herramientas del agente
# a Claude Code por el protocolo estándar. Es la única dependencia real.
mcp[cli]>=1.2.0

# El resto del proyecto (clases POO, predicción, RAG, correo) usa solo la
# librería estándar. Estas quedan como referencia para futuras ampliaciones:

# pandas            # manejo de datos / series de sensores
# matplotlib        # graficar las series
# langchain         # framework del agente (S4)
# faiss-cpu         # RAG en local (S4)
# python-dotenv     # leer variables de .env
